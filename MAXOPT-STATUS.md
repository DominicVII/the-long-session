# MAX OPT 20260921a — publish status

Playable HTML (`session.html` / `index.html` / `One Nation, Under, ME.html`) is **ready on the box** at `/workspace/the-long-session/` with stamp **`20260921a`**, `node --check` OK, trio byte-identical.

**Not yet on GitHub main** (still `20260920w`): `gh` PAT lacks `contents:write`; MCP cannot carry ~1.8MB HTML payloads; Cloud Agents unavailable on plan.

Parent: push the three HTML files (+ full `CHANGELOG.md` from the box) with a write-capable credential. Docs partially updated (`GROKBOT-HANDOFF.md` stamp `20260921a`; CHANGELOG weekly entry present).
